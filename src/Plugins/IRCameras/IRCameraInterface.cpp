#include "IRCameraInterface.h"

//#include "TestGLOffscreen.h"

IRCameraInterface::LoadResult IRCameraInterface::load() 
{
	/*ExamplePaintSurface paintSurface;
	paintSurface.resize(300, 200);
	paintSurface.render();
	QImage image = paintSurface.grabFramebuffer();
	image.save(QString("image.png"));

	paintSurface.render();
	image = paintSurface.grabFramebuffer();
	image.save(QString("image2.png"));*/

	return IRCameraInterface::Success;
}